[pytest]
# Look for tests in the tests directory
testpaths = tests

# Look for files matching the pattern test_*.py or *_test.py
python_files = test_*.py *_test.py

# Add options for verbosity, etc. if desired
# addopts = -v

# Specify the source directory for coverage reporting (if used)
# addopts = --cov=src
